package Programacion.Java.ReadMyFilesPleaseee.ejercicio5;
import java.io.File;

public class ReadFile5 {
    public static void main(String[] args) {
/*
        // Instaciem a la classe FILE amb les rutes relatives a les carpetes que volem esborrar
        File fotografies = new File("Documentos/Fotografias");
        File libros = new File("Documentos/Libros");
        File documentos = new File("Documentos");

        boolean resultat = false;

        try {
            // Borramos la carpeta 'Fotografias' y to' su contenido
            resultat = borrarTodo(fotografies);
            if (resultat) {
                System.out.println("La carpeta 'Fotografias' ha segut esborrada");
            }

            // Esborrem la carpeta 'Libros' i tot el seu contingut
            resultat = borrarTodo(libros);
            if (resultat) {
                System.out.println("La carpeta 'Libros' ha segut esborrada");
            }

            // Esborrem la carpeta 'Documentos' i tot el seu contingut
            resultat = borrarTodo(documentos);
            if (resultat) {
                System.out.println("La carpeta 'Documentos' ha sido borrada");
            }

        } catch (FileNotFoundException e) {
            System.out.println(e);
        }*/
    }
}


/* Crea una función llamada borrarTodo() que elimine todos los archivos y carpetas de una ruta:
 *
 * Si no existe la ruta, muestra una excepción.
 * Si es un archivo, lo borramos
 * Si es una carpeta, primero borramos todos sus archivos, y después, borramos la carpeta
 *
 */
